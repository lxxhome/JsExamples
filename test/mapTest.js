let numbers=[1,3,3,5,9,8];
console.log(numbers.map(Math.sqrt))
console.log(`===========`)
console.log(numbers)

// let redis=require('redis');
// let client=redis.createClient(6379,"127.0.0.1");
// client.info((err)=>{console.log(err)})

// client.hgetall('token:')